# isConkLive Rust edition

Small application to scrape viewer count from a TikTok live page and report it via a Discord webhook.


## Usage

    cargo build
    export DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/...", ./isconklive-rust
